# SEO

## Current Coverage

- Production canonical domain: `https://event-io.me`
- Metadata and canonical tags are set in the SPA shell and per-route `SeoHead` helper
- `robots.txt` and `sitemap.xml` point to the production domain

## Important Pages

- `/` — landing page
- `/calendar` — searchable event surface
- `/docs` — interactive API docs
- `/architecture` — system overview
- `/privacy`, `/terms`, `/cookies`, `/api-usage`, `/disclaimer`, `/about`, `/contact`, `/security`, `/opensource`
