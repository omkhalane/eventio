# Web App

React + Vite frontend for browsing and filtering developer events.

Run from the repository root:

```bash
npm run dev
npm run build
npm run typecheck
```

The root `vite.config.ts` sets this directory as the Vite app root so the
monorepo can keep shared tooling at the top level.
