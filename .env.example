# Browser-readable runtime config.
# These values are served from /api/config and are visible to users.
# Never put private keys, service-role keys, or passwords in PUBLIC_* variables.

# Supabase public client
PUBLIC_SUPABASE_URL=your_supabase_url
PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional server-only database connection.
# Only needed for Prisma commands such as npm run db:migrate / npm run db:studio.
DATABASE_URL=

# Firebase public web app config
PUBLIC_FIREBASE_PROJECT_ID=your_project_id
PUBLIC_FIREBASE_APP_ID=your_app_id
PUBLIC_FIREBASE_API_KEY=your_api_key
PUBLIC_FIREBASE_AUTH_DOMAIN=your_auth_domain
PUBLIC_FIREBASE_STORAGE_BUCKET=your_storage_bucket
PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
PUBLIC_FIREBASE_FIRESTORE_DATABASE_ID=your_firestore_database_id

# Server
NODE_ENV=development
PORT=3000

# Optional public analytics.
# Browser-readable project identifier, not a private secret.
PUBLIC_POSTHOG_KEY=
PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
